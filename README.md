
To run this application, execute the following commands:

  1. Install: 

    ```
    $ npm install
    ```

  2. Start the development:

    ```
    $ npm run watch
    ```  

  4. Make Build: 

    ```
    $ npm run build
    ```
